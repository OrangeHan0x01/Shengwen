示例视频使用的是一个87MB的MP4视频作为输入，处理在我的计算机上花费数十分钟（主要耗时在api语音识别和后续字幕合成上），对于一些用户视频输入动辄数百MB甚至几个G的大小，我不确认能否一步到位，这个问题主要可能出现在计算机内存大小有限制的情况下，但是事实上这种程序本身耗费性能就远小于有GUI界面的各类视频处理软件，一般能用gui处理的也能用此文件处理。因此我建议用户如果用虚拟机处理，尽量给虚拟机分配较大内存，实在不行可以将视频拆分成两个及以上来运行。（模块中只做了音频文件切分的函数而未做视频文件切分的函数，因此需要外部工具处理）
